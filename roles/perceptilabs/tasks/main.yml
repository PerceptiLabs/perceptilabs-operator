---
- name: Deploy PerceptiLabs backend microservice (core)
  k8s:
    state: present
    definition: "{{ lookup('template', 'core.yaml.j2') }}"

- name: Deploy PerceptiLabs frontend microservice
  k8s:
    state: present
    definition: "{{ lookup('template', 'frontend.yaml.j2') }}"
